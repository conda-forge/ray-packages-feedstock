From ad3baa62d67e247abf54212ccddc13a21ea23783 Mon Sep 17 00:00:00 2001
From: Kai Fricke <kai@anyscale.com>
Date: Wed, 4 Aug 2021 11:39:06 +0100
Subject: [PATCH 08/12] Add workaround for os.path.isdir on Windows

---
 python/setup.py | 20 ++++++++++++++++++++
 1 file changed, 20 insertions(+)

diff --git a/python/setup.py b/python/setup.py
index 02dcbb189..df87c1d42 100644
--- a/python/setup.py
+++ b/python/setup.py
@@ -230,6 +230,25 @@ def download_pickle5(pickle5_dir):
                 wzf.close()
 
 
+def patch_isdir():
+    '''
+    Python on Windows is having hard times at telling if a symlink is
+    a directory - it can "guess" wrong at times, which bites when
+    finding packages. Replace with a fixed version which unwraps links first.
+    '''
+    orig_isdir = os.path.isdir
+    def fixed_isdir(path, debug=False):
+        orig_path = path
+        while os.path.islink(path):
+            try:
+                link = os.readlink(path)
+            except OSError as err:
+                break
+            path = os.path.abspath(os.path.join(os.path.dirname(path), link))
+        return orig_isdir(path)
+    os.path.isdir = fixed_isdir
+
+
 def replace_symlinks_with_junctions():
     _LINKS = {
         r'ray\new_dashboard': '../../dashboard',
@@ -272,6 +291,7 @@ def replace_symlinks_with_junctions():
 
 
 if is_native_windows_or_msys():
+    patch_isdir()
     replace_symlinks_with_junctions()
 
 
-- 
2.26.2

